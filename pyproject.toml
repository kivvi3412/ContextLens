[project]
name = "contextlens"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "django>=5.2.5",
    "openai",
    "django-cors-headers",
    "uvicorn[standard]>=0.35.0",
]
